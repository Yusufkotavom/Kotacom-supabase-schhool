  {productData.content && (
    <section class="bg-white dark:bg-gray-900">
      <div class="max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">Informasi Tambahan</h2>
        <div class="prose md:prose-lg max-w-none text-gray-700 dark:text-gray-300" set:html={productData.content} />
      </div>
    </section>
  )}

  <!-- Product Review -->
  {productData.review && (
    <section class="bg-blue-50 dark:bg-blue-900">
      <div class="max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="text-center">
          <h2 class="text-2xl font-bold text-blue-900 dark:text-white mb-6">Ulasan / Review</h2>
          <div class="prose md:prose-lg max-w-3xl mx-auto text-blue-900 dark:text-blue-100" set:html={productData.review} />
        </div>
      </div>
    </section>
  )}