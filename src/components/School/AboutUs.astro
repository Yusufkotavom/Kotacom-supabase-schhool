---
const aboutData = {
  title: "Tentang Kami",
  subtitle: "SD Muhammadiyah 20 Surabaya",
  description: "Kami adalah sekolah dasar yang berkomitmen untuk mengembangkan kreativitas dan potensi setiap siswa melalui pendidikan berkualitas dengan pendekatan modern dan inovatif.",
  stats: [
    { number: "500+", label: "Siswa Aktif", icon: "👥" },
    { number: "25+", label: "Guru Berpengalaman", icon: "👨‍🏫" },
    { number: "15+", label: "Ekstrakurikuler", icon: "🎨" },
    { number: "95%", label: "Tingkat Kelulusan", icon: "🎓" }
  ],
  features: [
    {
      icon: "🎯",
      title: "Pendidikan Berkualitas",
      description: "Kurikulum nasional yang diperkaya dengan program pengembangan karakter dan kreativitas"
    },
    {
      icon: "🌟",
      title: "Pengembangan Kreativitas",
      description: "Program khusus untuk mengembangkan bakat dan minat siswa dalam berbagai bidang"
    },
    {
      icon: "🤝",
      title: "Pembentukan Karakter",
      description: "Pendidikan karakter yang mengintegrasikan nilai-nilai Islam dan nasionalisme"
    },
    {
      icon: "🌍",
      title: "Persiapan Global",
      description: "Persiapan siswa untuk menghadapi tantangan global dengan kemampuan bahasa dan teknologi"
    }
  ]
};
---

<section id="about" class="about-section">
  <div class="container mx-auto px-4 py-16">
    <div class="about-header text-center mb-16" data-aos="fade-up">
      <h2 class="section-title">{aboutData.title}</h2>
      <h3 class="section-subtitle">{aboutData.subtitle}</h3>
      <p class="section-description">{aboutData.description}</p>
    </div>
    
    <!-- Stats Section -->
    <div class="stats-grid mb-16" data-aos="fade-up" data-aos-delay="200">
      {aboutData.stats.map((stat, index) => (
        <div class="stat-card" data-aos="zoom-in" data-aos-delay={index * 100}>
          <div class="stat-icon">{stat.icon}</div>
          <div class="stat-number">{stat.number}</div>
          <div class="stat-label">{stat.label}</div>
        </div>
      ))}
    </div>
    
    <!-- Features Section -->
    <div class="features-grid">
      {aboutData.features.map((feature, index) => (
        <div class="feature-card" data-aos="fade-up" data-aos-delay={index * 200}>
          <div class="feature-icon">{feature.icon}</div>
          <h4 class="feature-title">{feature.title}</h4>
          <p class="feature-description">{feature.description}</p>
        </div>
      ))}
    </div>
    
    <!-- School Image -->
    <div class="school-image-container mt-16" data-aos="fade-up">
      <div class="school-image">
        <img src="/images/school/about-school.jpg" alt="SD Muhammadiyah 20 Surabaya" />
        <div class="image-overlay">
          <div class="overlay-content">
            <h4>Membangun Masa Depan Cemerlang</h4>
            <p>Bersama kami, setiap anak akan berkembang menjadi pribadi yang kreatif, berkarakter, dan siap menghadapi tantangan masa depan.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    position: relative;
    overflow: hidden;
  }
  
  .about-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="%23e2e8f0" opacity="0.1"/><circle cx="75" cy="75" r="1" fill="%23e2e8f0" opacity="0.1"/><circle cx="50" cy="10" r="0.5" fill="%23e2e8f0" opacity="0.1"/><circle cx="10" cy="60" r="0.5" fill="%23e2e8f0" opacity="0.1"/><circle cx="90" cy="40" r="0.5" fill="%23e2e8f0" opacity="0.1"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
    pointer-events: none;
  }
  
  .section-title {
    font-size: 3rem;
    font-weight: 800;
    background: linear-gradient(45deg, #3B82F6, #F59E0B);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
  }
  
  .section-subtitle {
    font-size: 1.5rem;
    color: #64748b;
    margin-bottom: 1rem;
    font-weight: 600;
  }
  
  .section-description {
    font-size: 1.1rem;
    color: #64748b;
    max-width: 800px;
    margin: 0 auto;
    line-height: 1.8;
  }
  
  /* Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
  }
  
  .stat-card {
    background: white;
    padding: 2rem;
    border-radius: 20px;
    text-align: center;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
    border: 2px solid transparent;
    position: relative;
    overflow: hidden;
  }
  
  .stat-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(45deg, #3B82F6, #F59E0B);
  }
  
  .stat-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    border-color: #3B82F6;
  }
  
  .stat-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
  
  .stat-number {
    font-size: 2.5rem;
    font-weight: 800;
    color: #3B82F6;
    margin-bottom: 0.5rem;
  }
  
  .stat-label {
    font-size: 1rem;
    color: #64748b;
    font-weight: 600;
  }
  
  /* Features Grid */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
  
  .feature-card {
    background: white;
    padding: 2rem;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }
  
  .feature-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.05), rgba(245, 158, 11, 0.05));
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  
  .feature-card:hover::before {
    opacity: 1;
  }
  
  .feature-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }
  
  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
  
  .feature-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: #1e293b;
    margin-bottom: 1rem;
  }
  
  .feature-description {
    color: #64748b;
    line-height: 1.6;
  }
  
  /* School Image */
  .school-image-container {
    position: relative;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }
  
  .school-image {
    position: relative;
    height: 400px;
    overflow: hidden;
  }
  
  .school-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }
  
  .school-image:hover img {
    transform: scale(1.05);
  }
  
  .image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.8), rgba(245, 158, 11, 0.8));
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  
  .school-image:hover .image-overlay {
    opacity: 1;
  }
  
  .overlay-content {
    text-align: center;
    color: white;
    padding: 2rem;
  }
  
  .overlay-content h4 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }
  
  .overlay-content p {
    font-size: 1.1rem;
    line-height: 1.6;
  }
  
  @media (max-width: 768px) {
    .section-title {
      font-size: 2.5rem;
    }
    
    .section-subtitle {
      font-size: 1.3rem;
    }
    
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }
    
    .stat-card {
      padding: 1.5rem;
    }
    
    .stat-icon {
      font-size: 2.5rem;
    }
    
    .stat-number {
      font-size: 2rem;
    }
    
    .features-grid {
      grid-template-columns: 1fr;
    }
    
    .school-image {
      height: 300px;
    }
    
    .overlay-content h4 {
      font-size: 1.5rem;
    }
  }
</style>